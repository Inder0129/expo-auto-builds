import { StyleSheet } from 'react-native';
import { spacing } from '@/src/theme/spacing';

export const scientificStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: spacing.lg,
    justifyContent: 'space-between',
  },
});
