import { moderateScale } from './spacing';

const fontFamily = 'Inter';
const fontSizes = { xs: moderateScale(12), sm: moderateScale(14), md: moderateScale(16) };
export default { fontFamily, fontSizes };
