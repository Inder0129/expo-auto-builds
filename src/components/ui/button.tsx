import React from 'react'; import { Pressable, Text, useThemedStyles } from '@/theme'; interface ButtonProps { title: string; onPress: () => void; variant?: 'primary' | 'secondary'; loading?: boolean; } export const Button: React.FC<ButtonProps> = ({ title, onPress, variant = 'primary', loading }) => { const styles = useThemedStyles(); return <Pressable style={[styles.button, variant === 'primary' ? styles.primary : styles.secondary]} onPress={onPress}><Text style={styles.buttonText}>{title}</Text>{loading && <Text>Loading...</Text>}</Pressable>; };