import React, { useState } from 'react'; import { TextInput, Text, useThemedStyles } from '@/theme'; interface InputProps { label: string; error?: string; } export const Input: React.FC<InputProps> = ({ label, error }) => { const [value, setValue] = useState(''); const styles = useThemedStyles(); return <><Text style={styles.label}>{label}</Text><TextInput style={[styles.input, error && styles.error]} value={value} onChangeText={setValue} /><Text style={styles.errorText}>{error}</Text></>; };