name: Build Expo Android APK

on:
  push:
    branches: [main, master]
  workflow_dispatch:

# Cancel any in-progress builds when a new push arrives
concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Read build metadata
        id: meta
        run: |
          if [ -f .build-meta.json ]; then
            echo "chat_id=$(jq -r '.chatId' .build-meta.json)" >> $GITHUB_OUTPUT
            echo "app_name=$(jq -r '.appName' .build-meta.json)" >> $GITHUB_OUTPUT
            echo "has_meta=true" >> $GITHUB_OUTPUT
          else
            echo "has_meta=false" >> $GITHUB_OUTPUT
          fi

      - name: Install dependencies
        run: npm install

      # ============================================
      # PATH A: Try EAS Build first (free tier)
      # ============================================
      - name: Install EAS CLI
        run: npm install -g eas-cli

      - name: EAS Build (Path A)
        id: eas_build
        continue-on-error: true
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          echo "Attempting EAS Build..."

          # Run eas build and capture output
          if eas build --platform android --profile preview --non-interactive --json 2>eas-error.log | tee eas-output.json; then
            # Extract the build URL from JSON output
            BUILD_URL=$(jq -r '.[0].artifacts.buildUrl // empty' eas-output.json 2>/dev/null)

            if [ -n "$BUILD_URL" ] && [ "$BUILD_URL" != "null" ]; then
              echo "EAS Build succeeded!"
              echo "apk_url=$BUILD_URL" >> $GITHUB_OUTPUT
              echo "success=true" >> $GITHUB_OUTPUT
            else
              # Build might have finished but URL not in initial output
              # Try to get it from build ID
              BUILD_ID=$(jq -r '.[0].id // empty' eas-output.json 2>/dev/null)
              if [ -n "$BUILD_ID" ]; then
                echo "Waiting for build $BUILD_ID to complete..."
                for i in $(seq 1 60); do
                  sleep 30
                  BUILD_INFO=$(eas build:view "$BUILD_ID" --json 2>/dev/null || echo "{}")
                  STATUS=$(echo "$BUILD_INFO" | jq -r '.status // empty')

                  if [ "$STATUS" = "finished" ]; then
                    BUILD_URL=$(echo "$BUILD_INFO" | jq -r '.artifacts.buildUrl // empty')
                    echo "apk_url=$BUILD_URL" >> $GITHUB_OUTPUT
                    echo "success=true" >> $GITHUB_OUTPUT
                    echo "EAS Build completed!"
                    break
                  elif [ "$STATUS" = "errored" ]; then
                    echo "EAS Build errored"
                    echo "success=false" >> $GITHUB_OUTPUT
                    break
                  fi
                  echo "Build status: $STATUS (attempt $i/60)"
                done
              else
                echo "success=false" >> $GITHUB_OUTPUT
              fi
            fi
          else
            echo "EAS Build command failed"
            cat eas-error.log
            echo "success=false" >> $GITHUB_OUTPUT
          fi

      # ============================================
      # PATH B: Gradle Build fallback (unlimited)
      # ============================================
      - name: Setup Java (Gradle fallback)
        if: steps.eas_build.outputs.success != 'true'
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Setup Android SDK (Gradle fallback)
        if: steps.eas_build.outputs.success != 'true'
        uses: android-actions/setup-android@v3

      - name: Expo Prebuild (Gradle fallback)
        if: steps.eas_build.outputs.success != 'true'
        run: |
          echo "EAS Build failed/unavailable. Falling back to Gradle build..."
          npx expo prebuild --platform android --no-install

      - name: Gradle Build APK (Gradle fallback)
        if: steps.eas_build.outputs.success != 'true'
        id: gradle_build
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon 2>&1 || ./gradlew assembleDebug --no-daemon 2>&1
          cd ..

          # Find the APK
          APK_PATH=$(find android/app/build/outputs/apk -name "*.apk" | head -1)
          if [ -n "$APK_PATH" ]; then
            echo "apk_path=$APK_PATH" >> $GITHUB_OUTPUT
            echo "success=true" >> $GITHUB_OUTPUT
            echo "APK built: $APK_PATH"
          else
            echo "success=false" >> $GITHUB_OUTPUT
            echo "No APK found!"
          fi

      - name: Upload APK as Release (Gradle fallback)
        if: steps.eas_build.outputs.success != 'true' && steps.gradle_build.outputs.success == 'true'
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: "Build #${{ github.run_number }}"
          body: "Auto-generated APK build"
          files: ${{ steps.gradle_build.outputs.apk_path }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ============================================
      # Determine final APK URL
      # ============================================
      - name: Get final APK URL
        id: final_url
        run: |
          if [ "${{ steps.eas_build.outputs.success }}" = "true" ]; then
            echo "url=${{ steps.eas_build.outputs.apk_url }}" >> $GITHUB_OUTPUT
            echo "method=EAS Build" >> $GITHUB_OUTPUT
          elif [ "${{ steps.gradle_build.outputs.success }}" = "true" ]; then
            echo "url=https://github.com/${{ github.repository }}/releases/download/build-${{ github.run_number }}/$(basename ${{ steps.gradle_build.outputs.apk_path }})" >> $GITHUB_OUTPUT
            echo "method=Gradle Build" >> $GITHUB_OUTPUT
          else
            echo "url=FAILED" >> $GITHUB_OUTPUT
            echo "method=none" >> $GITHUB_OUTPUT
          fi

      # ============================================
      # Notify via Telegram
      # ============================================
      - name: Send success to Telegram
        if: steps.final_url.outputs.url != 'FAILED' && steps.meta.outputs.has_meta == 'true'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: |
          CHAT_ID="${{ steps.meta.outputs.chat_id }}"
          APP_NAME="${{ steps.meta.outputs.app_name }}"
          APK_URL="${{ steps.final_url.outputs.url }}"
          METHOD="${{ steps.final_url.outputs.method }}"

          MESSAGE="Your APK is ready!

App: ${APP_NAME}
Built via: ${METHOD}
Download: ${APK_URL}"

          curl -s -X POST \
            "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -H "Content-Type: application/json" \
            -d "$(jq -n --arg chat_id "$CHAT_ID" --arg text "$MESSAGE" '{chat_id: $chat_id, text: $text}')"

      - name: Send failure to Telegram
        if: steps.final_url.outputs.url == 'FAILED' && steps.meta.outputs.has_meta == 'true'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: |
          CHAT_ID="${{ steps.meta.outputs.chat_id }}"
          APP_NAME="${{ steps.meta.outputs.app_name }}"

          MESSAGE="Build failed for: ${APP_NAME}

Both EAS Build and Gradle Build failed. Check the Actions log:
https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          curl -s -X POST \
            "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -H "Content-Type: application/json" \
            -d "$(jq -n --arg chat_id "$CHAT_ID" --arg text "$MESSAGE" '{chat_id: $chat_id, text: $text}')"
